import 'package:flutter/material.dart';

class Xatchop extends StatelessWidget {
  const Xatchop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}