import 'package:flutter/material.dart';

class OvozPages extends StatelessWidget {
  const OvozPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(backgroundColor: Colors.green.shade700,title: Text('Xabarnoma ovozi'),),
      body: Column(children: [Text("")],),);
  }
}